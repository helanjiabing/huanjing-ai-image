# 幻境 AI 图片生成后台

此 Worker 使用 `gemini-3.1-flash-image`（Nano Banana 2），密钥通过 Cloudflare Secret 保存，不会进入 GitHub 仓库。

双击 `deploy.command`，依次输入 Cloudflare API Token 和 Gemini API Key。完成后复制终端显示的 `workers.dev` 地址。
